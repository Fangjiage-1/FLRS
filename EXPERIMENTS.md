# ELF 采样方法实验报告

**模型**: ELF-B (105M, 5 epochs) / ELF-L (652M, 3 epochs) / ELF-M (342M, 4 epochs)  
**数据**: OpenWebText, T5-small 编码器  
**评估指标**: GPT-2 Large PPL (perplexity) ↓ 越低越好 / Unigram Entropy ↑ 越高越好  
**固定参数**: CFG=1, SC-CFG=3, time_schedule=logit_normal, seed=42  
**NFE** = Number of Function Evaluations = 网络前向总次数

---

## 一、基线方法

### 1.1 SDE (原版 ELF)

**算法**：每步执行 (1) 噪声回退 (2) 欧拉前向去噪。SDE 形式的离散 Euler-Maruyama 步：

```
h = t_next - t
alpha = 1 - gamma * h                    # 信号保留比例
t_back = alpha * t                        # 回退到的时间点
z_back = alpha * z + (1-alpha) * noise   # 加噪回退
v_pred = model(z_back, t_back)           # 预测速度场
z_next = z_back + (t_next - t_back) * v_pred  # 欧拉前向
```

gamma 控制每步噪声注入强度。gamma=0 时退化为一阶 Euler ODE。

| 步数 | NFE | gamma | PPL | Entropy |
|------|-----|-------|-----|---------|
| 32 | 32 | 1.5 | 23.75 | 5.15 |
| 64 | 64 | 1.0 | 19.22 | 5.08 |

**动机**：这是原版论文的默认方法，是本实验的基线。噪声注入使得样本在多个模态间随机游走，天然提供多样性 (Entropy)。

**关键观察**：原版 SDE 的最佳 gamma 随步数变化——32 步使用 gamma=1.5，64 步使用 gamma=1.0。一种可能的解释是粗步长 (N=32) 的离散误差较大，需要更强的噪声来"打散"积分误差的累积。

### 1.2 ODE (一阶前向欧拉)

**算法**：纯确定性积分 `z_next = z + Δt · v_pred(z, t)`，无噪声。

论文中使用 SDE 作为默认采样方式，ODE 主要作为对比项。

| 步数 | NFE | PPL | Entropy |
|------|-----|-----|---------|
| 64 | 64 | 27.55 | 5.17 |

**发现**：确定性 Euler ODE 的 PPL (27.55) 与 2 阶 Heun (28.42) 几乎持平，说明在 FM 文本空间中提高积分阶数几乎不带来收益——积分精度不是瓶颈。

---

## 二、确定性高阶求解器

> **研究动机**：如果数值积分精度是主要瓶颈，提高求解器阶数应该能在同等 NFE 下获得更好的 PPL。图像扩散领域（EDM、DPM-Solver）已验证了这一点。

### 2.1 Heun (速度场 2 阶 Runge-Kutta)

**算法**：改进欧拉法，每步 2 次模型评估。先用欧拉预测，再在预测点校正：

```
Stage 1 (predictor):
  t_batch = full([t, t, ...])
  v1, xp1 = model(z, t)
  z_pred = z + h * v1                   # 欧拉预测步

Stage 2 (corrector):
  t_next_batch = full([t_next, t_next, ...])
  v2, x_pred = model(z_pred, t_next)    # 在预测点重新评估
  z_next = z + h/2 * (v1 + v2)          # 梯形校正
```

每步 2 NFE。在光滑 ODE 上全局误差为 O(h²)（vs 欧拉的 O(h)），但 Flow Matching 的速度场在 t→1 附近有奇异性，实际阶数可能低于理论值。额外的一次模型调用用于"校正"预测步的偏差。

| 步数 | NFE | PPL | Entropy |
|------|-----|-----|---------|
| 16 | 32 | 50.37 | 5.34 |
| 32 | 64 | 28.42 | 5.18 |
| 64 | 128 | 17.87 | 5.02 |

**发现**：同 NFE (32) 下 Heun 的 PPL=50 远差于 SDE 的 23.75。2 阶精度提升无法弥补噪声缺失。只有当 NFE=128 (Heun 64 步) 时才勉强超过 SDE 64 步 (NFE=64)，性价比极差。**确定性高阶积分在 Flow Matching 的奇异性速度场下优势被大幅削弱。**

### 2.2 指数积分 (exp)

**算法**：Flow Matching 的 ODE 是半线性的：`dz/dt = -z/(1-t) + x0/(1-t)`。将 x0_pred 视为分段常数，线性部分有解析解：

```
x0 = model(z, t)
ratio = (1 - t_next) / (1 - t)
z_next = x0 + (z - x0) * ratio          # 精确解 (x0 恒定假设下)
```

当 t_next=1 时 ratio=0，z_next = x0——**奇异性被精确消除**。每步 1 NFE（和欧拉相同）。

| 步数 | NFE | PPL | Entropy |
|------|-----|-----|---------|
| 16 | 16 | 101.10 | 5.48 |
| 32 | 32 | 46.65 | 5.31 |

**发现**：比同 NFE 的 Euler 好，但仍远不如 SDE。指数积分在 t→1 处精确，但 t<1 时的"x0 分段常数"假设引入的误差仍大于 SDE 噪声校正后的残余误差。**准确 ≠ 高质量：SDE 的"有噪声的近似"比 ODE 的"精确近似"产生更自然的文本。**

### 2.3 指数 Heun (exp_heun, 2 阶)

**算法**：指数积分 + 中点校正（类似 DPM-Solver-2 for diffusion models）：

```
Stage 1:
  x0_1 = model(z, t)
  z_mid = x0_1 + (z - x0_1) * (1-t_mid) / (1-t)   # 解析半步（基点=原始 z）

Stage 2:
  x0_2 = model(z_mid, t_mid)                        # 在中点重估 x0
  z_next = x0_2 + (z - x0_2) * (1-t_next) / (1-t)  # 解析全步（基点=原始 z）
```

注意两次指数积分都以原始 z 为基点——只替换了 x0 的估计值，这是指数中点法的正确形式（类似 DPM-Solver-2）。

每步 2 NFE。

| 步数 | NFE | PPL | Entropy |
|------|-----|-----|---------|
| 16 | 32 | 46.25 | 5.32 |
| 32 | 64 | 27.36 | 5.17 |

**发现**：指数积分 ≈ 速度场 Heun，两种高阶方案在同 NFE 下非常接近。**在 ELF 框架下，积分方式（速度场 vs 指数）不是主导因素。**

---

## 三、SDE-ODE 混合方法

> **研究动机**：SDE 噪声强但最后几步破坏收敛，ODE 干净但缺乏多样性。能否在积分过程中切换，取两者之长？

### 3.1 SDE-Exp 混合 (sde_exp)

**算法**：以阈值 θ 分割时间轴。θ 之前用 SDE（噪声提供多样性 + 误差校正），θ 之后用指数积分（解析处理 t→1 奇异性）。最后一步始终使用指数积分。

```
for each (t, t_next):
    if t < threshold:
        z, xp = sde_step(z, t, t_next)   # 加噪 → 回退 → 欧拉
    else:
        z, xp = exp_step(z, t, t_next)   # 确定性指数积分
# Final step: exp_step 到 t=1
```

| 步数 | NFE | gamma | threshold | PPL | Entropy |
|------|-----|-------|-----------|-----|---------|
| 32 | 32 | 1.5 | 0.7 | 23.98 | 5.16 |
| 32 | 32 | 1.5 | 0.8 | 24.50 | 5.16 |
| 64 | 64 | 1.0 | 0.8 | 19.06 | 5.08 |

**发现**：与纯 SDE 基本持平。切换到 exp 几乎无影响——SDE 的前期噪声已经主导了整个轨迹的质量。**在 SDE 已充分混合的前提下，后期求解器的选择无关紧要。**

### 3.2 Predictor-Corrector (PC)

**算法**：EDM 风格的 PC 采样器。Predictor 用 exp 确定性推一步，Corrector 在终点处加噪再拉回（起到 Langevin MCMC 校正作用）：

```
Predictor:
  z_pred = exp_step(z, t, t_next)           # 确定性

Corrector:
  z_noise = alpha * z_pred + (1-alpha) * noise  # 加噪
  z_next  = exp_step(z_noise, t_back, t_next)    # 拉回
```

每步 2 NFE。Corrector 的 gamma_c 远小于纯 SDE 的 gamma (0.3-0.5 vs 1.0-1.5)。

| 步数 | NFE | gamma_c | PPL | Entropy |
|------|-----|---------|-----|---------|
| 8 | 16 | 0.3 | 95.56 | 5.47 |
| 8 | 16 | 0.5 | 81.49 | 5.43 |
| 16 | 32 | 0.3 | 40.78 | 5.28 |
| 16 | 32 | 0.5 | 37.92 | 5.26 |
| 32 | 64 | 0.3 | 23.97 | 5.13 |

**发现**：PC 是同 NFE 下最好的非纯 SDE 方法，但仍被 SDE 碾压（32 NFE: 37.92 vs 23.75）。原因：Corrector 的噪声量级太小，只做"微调"而非真正的多模态探索。**噪声必须足量——proactive noise (SDE) >> reactive correction (PC)。**

### 3.3 Multi-Langevin SDE (sde_ml)

**算法**：每步执行 k 轮独立的 {加噪回退 → 指数积分去噪}，试图通过多轮迭代增强随机校正：

```
for round in range(k):
    noise = randn_like(z)
    z_noise = alpha * z + (1-alpha) * noise
    z = exp_step(z_noise, t_back, t_next)
```

每步 k NFE。所有 k 轮的噪声独立采样。

| 步数 | NFE | gamma | k | PPL | Entropy | 状态 |
|------|-----|-------|---|-----|---------|------|
| 16 | 32 | 1.5 | 2 | 11.68 | 4.76 | ⚠️ 部分坍缩 |
| 16 | 48 | 1.5 | 3 | 3.27 | 3.07 | 💀 严重坍缩 |
| 32 | 64 | 1.0 | 2 | 12.23 | 4.85 | ⚠️ 部分坍缩 |

**发现**：多轮 Langevin 导致 **mode collapse**——文本退化为来回重复的 token（"the the the..."、"password is located on the back of the phone" 循环）。k=3 时 PPL=3.27 看起来极低，但这是坍缩假象（GPT-2 轻易预测循环文本）。一种可能的解释是：在同一时间步反复以同一模型做加噪→去噪，每次去噪都将 z 推向模型最确信的方向，k 次迭代构成了对该方向的重复强化——等效于梯度下降在潜空间收敛到一个尖锐的局部极小值。

---

## 四、自适应步长与解码约束

> **研究动机**：t→1 附近的速度场有奇异性 (1/(1-t) → ∞)，固定步长的离散误差在此处最大。自适应步长可以自动在奇异区域加密采样，在其他区域保持大步长。

### 4.1 自适应 Heun (heun_adaptive)

**算法**：Heun 的 2 次模型调用天然提供了误差估计。比较欧拉预测 z_euler 和 Heun 校正 z_heun：

```
z, xp, err = heun_step_with_err(z, t, t_next)
           # err = (h/2) * max(||v2 - v1|| / ||z||)

if err <= tol * h:    # 误差在容限内 → 接受
    return z, xp
else:                  # 误差过大 → 递归二分
    z_mid = adaptive_step(z, t, t_mid, tol)
    return adaptive_step(z_mid, t_mid, t_next, tol)
```

误差估计完全免费——v1 和 v2 来自 Heun 的两次评估。tol 越小，t→1 附近细分越多。`max_depth=4` 防止无限递归。

| tol | 基础步数 | PPL | Entropy |
|-----|---------|-----|---------|
| 0.1 | 32 | 13.83 | 4.98 |
| 0.05 | 32 | 10.42 | 4.86 |
| 0.02 | 32 | 9.20 | 4.79 |

**发现**：PPL 随着 tol 收紧而大幅下降——自适应细分确实消除了 t→1 附近的积分误差。但 **Entropy 同步下降**：越精确的确定性积分，越将 latent 推向一个单一的"最佳"模态。注意这并非 PPL 和 Entropy 之间存在数学上的互斥约束——更合理的解释是 ELF-B 的噪声机制同时驱动两者同向移动（噪声多→高 PPL+高 Entropy，噪声少→低 PPL+低 Entropy）。更好的模型（更大规模、更长训练）预期可以同时获得更低 PPL 和更高 Entropy。

### 4.2 解码约束 (temperature + repetition penalty)

**动机**：如果确定性采样器产生了低熵 latent，能否在解码阶段通过 token 级后处理恢复多样性？

**算法**：在自适应 Heun 的基础上，解码时使用 temperature scaling + repetition penalty：

```
# Temperature scaling (T < 1 使分布更尖锐，T > 1 更平坦)
logits = decoder(z) / T

# Repetition penalty: 已出现过的 token 被降权
for each token_id seen before:
    logits[token_id] -= log(penalty) * count_seen

# Softmax + 采样
probs = softmax(logits)
token = sample(probs)
```

| tol | T | rp | PPL | Entropy |
|-----|---|----|-----|---------|
| 0.05 | 0.8 | 1.2 | 10.20 | 4.86 |
| 0.02 | 0.8 | 1.2 | 9.41 | 4.81 |

**发现**：在测试的 T=0.8 和 rp=1.2 配置下基本没有效果。原因：如果 latent z 已经坍缩到一个唯一模态（自适应 Heun 的严格 tol 导致），那么各类 token 的 logits 已经极度悬殊——temperature 和 repetition penalty 都在试图从一张空信息的光谱中挤出多样性。**信息在潜空间丢失后，token 级后处理是徒劳的。**

### 4.3 自条件噪声 (sc_noise)

**动机**：借鉴 LeWM (LeCun et al., 2026) 中 SIGReg 的各向同性高斯约束。不扰动 z 本身，而是扰动模型对自己"目标画面"(x_pred) 的感知，引导模型看到不同的去噪方向：

```
x_pred_noisy = x_pred + sc_noise * (1-t)² * randn()
z_input = concat([z, x_pred_noisy])     # 自条件输入被扰动
v, x_new = model(z_input)               # 模型看到"模糊的自我画像"
```

噪声在 t→1 时衰减为 0。只影响自条件通道。

| tol | sc_noise | PPL | Entropy |
|-----|----------|-----|---------|
| 0.05 | 0.05 | 10.32 | 4.86 |
| 0.05 | 0.1 | 10.03 | 4.84 |
| 0.05 | 0.2 | 9.88 | 4.83 |
| 0.02 | 0.1 | 9.00 | 4.76 |

**发现**：**无效，且大噪声导致坍缩。**

| sc_noise | 0.05 | 0.1 | 0.2 | 0.5 | 1.0 |
|----------|------|-----|-----|-----|------|
| PPL | 10.32 | 10.03 | 9.88 | 8.46 | 5.35 |
| Entropy | 4.86 | 4.84 | 4.83 | 4.70 | 3.36 |

小噪声 (≤0.2) 被模型完全忽略，文本质量与裸 Heun 无异。大噪声 (≥0.5) 直接导致模式坍缩——PPL 下降是坍缩假象，Entropy 暴跌证实了这一点。原因：x_pred 是模型对"干净数据"的自我估计，扰动 x_pred 等价于向模型提供错误的目标信号；低剂量时模型从 z 通道已有足够信息重构正确输出忽略扰动，高剂量时目标信号被污染到不可恢复、模型退化为输出高频 token。

**sc_noise 不存在有效操作窗口。它既不是正则化（小剂量无作用），也不是多样性来源（大剂量坍缩）。这与 z 空间 SDE 噪声形成根本对比：z 噪声是模型训练分布内的信号，模型知道如何处理；x_pred 噪声是分布外扰动，模型只有忽略或崩溃两种反应。**

### 4.4 z 空间噪声 (z_noise)

**动机**：sc_noise 失败是因为扰动加在模型可以忽略的通道上。直接在 latent state z 上加噪声——模型训练就是去处理 z 空间的高斯噪声的，无法忽略。

```
# 每步后、下一步前
z = z + z_noise * (1-t_next)² * randn_like(z)
```

噪声同样在 t→1 时衰减。

| tol | z_noise | PPL | Entropy |
|-----|---------|-----|---------|
| 0.05 | 0.05 | 10.21 | 4.85 |
| 0.05 | 0.1 | 10.25 | 4.84 |
| 0.05 | 0.2 | 10.19 | 4.82 |
| 0.02 | 0.1 | 9.39 | 4.79 |

**发现**：**仍然无效**。虽然 z_noise 作用在正确空间，但衰减因子 (1-t)² 和 z_noise=0.05-0.2 的组合导致有效噪声量级仅占 z 的 1-5%。在自适应 Heun 的强力确定性动力学面前，这一量级的噪声被积分精度完全覆盖。**要做 SDE 就做真正的 SDE——gamma=1.0-2.0 级别，而不是小打小闹的 0.05。**

---

## 五、SDE Annealing — ✅ 最优方法

> **研究动机**：前面的实验揭示了两个核心事实：(1) SDE 噪声是多样性的唯一来源；(2) 自适应步长可以消除奇异点附近的积分误差。**退火 SDE 将二者统一：在 t≈0 区域用强 SDE 噪声做多模态探索，在 t→1 区域退火至零噪声做干净收敛。**
>
> **命名约定**：本文中 **SDE annealing** 特指推荐的 $\gamma_{\text{start}}{=}2.0 \to \gamma_{\text{end}}{=}0$ 配方。$\gamma_{\text{start}}{=}1.5$ 的弱化版本称为 **mild annealing**，仅用于消融对比。

### 算法

```
gamma(t) = gamma_end + (gamma_start - gamma_end) * (1 - t)^p

for each step:
    gamma_eff = gamma(t)               # 当前时间步的噪声强度
    sde_step(z, t, t_next, gamma_eff)  # 标准 SDE，但 gamma 随时间变化
```

退火协议：
- **p=1 (线性)**：噪声在全程均匀衰减。t=0.5 时 gamma = gamma_start/2。
- **p=2 (二次)**：噪声在早期保持较高（t=0.5 时 gamma = gamma_start/4），后期加速衰减。适合需要长时间保持多样性的场景。

**与纯 SDE 的关键区别**：纯 SDE 在最后几步仍注入 gamma=1.0 的噪声，这直接在 z 上增加了随机偏移，使得最终时刻的 z(1) 并非精确的 x0_pred，而是含有残余噪声的近似值。退火 SDE 在 t→1 时 gamma→0（消除噪声注入），且最后一步切换为指数积分（精确保留 z→x0 的解析结构），双重保证干净收敛。

### 结果总表

| 步数 | NFE | gamma | 退火 | PPL | Entropy | 状态 |
|------|-----|-------|------|-----|---------|------|
| 32 | 32 | 2.0→0 | 线性 | **21.47** | 5.10 | 🏆 32 NFE 最优 |
| 64 | 64 | 2.0→0 | 线性 | **15.71** | 5.01 | 🏆 最低 PPL |
| 64 | 64 | 2.0→0 | p=2 | 16.58 | 5.04 | |
| 256 | 256 | 2.0→0 | 线性 | 9.08 | 4.82 | 💀 坍缩 |

**核心发现**：

1. **SDE annealing 在 32-64 步范围内全面优于标准 SDE。** 32 NFE 下 PPL=21.47 比原版 SDE（PPL=23.75）提升 9.6%；64 NFE 下 PPL=15.71 比原版 SDE（PPL=19.22）提升 18.3%，为全局最低。

2. **256 步坍缩。** 当 N→∞ 时 SDE 退化为 ODE，噪声被步长稀释。实验结果与 THEORY.md 命题 2 的猜想一致。

3. **线性退火 (p=1) 优于二次 (p=2)。** p=2 的 PPL=16.58 vs p=1 的 15.71。一个可能的解释是 (1-t)² 在 t 仍较小时就将 gamma 抑制到很低的水平，减少了可用的总噪声预算。

### 消融研究：噪声预算 vs 时域分配

SDE annealing 的核心设计涉及两个交织因素：(1) 总噪声预算 ∫γ(t)dt；(2) 预算的时域分配。以下消融实验将二者解耦：

| 配置 | γ_start | ∫γ 总预算 | PPL (最佳步数) | vs 对应等预算 SDE |
|------|--------|----------|---------------|-------------------|
| **SDE annealing** | 2.0 | 1.0 | **15.71** (64步) | SDE γ=1.0: 19.22 → **-18.3%** |
| SDE γ=1.0 (等预算) | 1.0 (恒定) | 1.0 | 19.22 (64步) | 基线 |
| mild anneal. γ_start=1.5 | 1.5 | 0.75 | 25.30 (32步) | SDE γ=1.5: 23.75 → **+6.5% 变差** |
| mild anneal. γ_start=1.0 | 1.0 | 0.50 | 19.62 (64步) | SDE γ=1.0: 19.22 → **-2.1%** |

**三项发现**：

1. **预算不是唯一因素。** SDE annealing 和恒定 SDE γ=1.0 共享等量总预算（∫γ=1.0），但退火版 PPL 改善 18.3%。增益完全来自重分配——将噪声集中在速度场平滑的 t≈0 区域，而非均匀散布。

2. **缩减预算会损害性能。** mild annealing γ_start=1.5（∫γ=0.75，仅为 SDE γ=1.5 预算的一半）在 32 步时 PPL=25.30 **比恒定 SDE γ=1.5 还差**（23.75）。晚期噪声虽被消除（这是有收益的），但总预算 0.75 不足以支撑早期充分探索。γ_start=1.0（∫γ=0.50）64 步 PPL=19.62，与等预算的恒定 SDE γ=1.0（PPL=19.22）持平——**当预算太小时，仅靠重分配无法弥补。**

3. **SDE annealing 是"纯重分配"，不是"加更多噪声"。** 对比恒定 SDE γ=1.0（∫γ=1.0）与 mild annealing γ_start=1.5（∫γ=0.75）：前者的等预算重分配 (-18.3%) 远优于后者的缩预算尝试 (+6.5%)。核心设计原则是：**噪声预算必须保持充足，然后通过时域前移将同量预算转化为更高的样本质量。**

4. **64 步是甜点。** 64 步的积分精度足够消除 Euler 误差，同时步长不会小到把 SDE 噪声稀释至坍缩。

### 128 步边界测试

在 SDE annealing 框架下，将步数进一步扩展到 128 步，系统性扫描噪声预算从零到标准值的完整谱系：

| γ_start | ∫γ 总预算 | PPL (128步) | Entropy (128步) | 状态 |
|---------|----------|------------|----------------|------|
| 0.0 (纯 ODE) | 0 | 17.62 | 5.02 | ✅ 128 NFE 确定性上限 |
| 0.5 | 0.25 | 15.63 | 4.99 | ⚠️ 文本开始重复 |
| 1.0 | 0.50 | 13.66 | 4.96 | ⚠️ |
| 1.5 | 0.75 | 12.46 | 4.93 | ⚠️ |
| 2.0 | 1.0 | 11.32 | 4.90 | 💀 PPL 欺骗性低但文本已坍缩 |

128 步下 PPL 从 0.0 到 2.0 严格递减，Entropy 也严格递减。但检查生成文本发现，即使 PPL=11.32 的 γ=2.0 配置也已出现明显循环重复，PPL 的改善是坍缩假象。γ=0.0→0 即纯指数积分 128 步，PPL=17.62 可作为该 NFE 下确定性采样的参考上限。

### 坍缩定量分析：Collapse Ratio

为客观区分"真坍缩"和"低熵但正常"，我们定义 **collapse ratio**：在生成文本中，出现 ≥3 次的 5-gram 所覆盖的 token 占总 token 的比例。正常文本的 collapse ratio 接近 0（没有大规模短语重复），坍缩文本显著高于 0.1。

| 步数 | collapse ratio (avg) | 坍缩样本比例 (>0.1) | PPL | Entropy |
|------|---------------------|--------------------|-----|---------|
| 32 | 0.010 | 0% | 21.47 | 5.10 |
| 64 | 0.042 | 8.5% | 15.71 | 5.01 |
| 128 | 0.127 | 51% | 11.32 | 4.90 |
| 256 | 0.221 | 80% | 9.08 | 4.82 |

32 步和 64 步坍缩比例均不高于 8.5%，collapse ratio 保持在 0.05 以下——处于健康区间。128 步和 256 步在 collapse ratio 和坍缩样本比例上均呈现断层式跳升（4.2% → 12.7% collapse ratio，8.5% → 51% 样本坍缩），验证了 64 步未坍缩的判断。

---

## 六、最终对比总表

按 NFE 对齐排序。同 NFE 区间内以 PPL 降序排列。

| 方法 | 步数 | NFE | PPL ↓ | Entropy ↑ | 状态 |
|------|------|-----|-------|-----------|------|
| exp | 16 | 16 | 101.10 | 5.48 | ❌ |
| pc γ=0.3 | 8 | 16 | 95.56 | 5.47 | ❌ |
| | | | | | |
| SDE γ=1.5 | 32 | 32 | **23.75** | 5.15 | ✅ 32 NFE 基线 |
| sde_exp θ=0.7 | 32 | 32 | 23.98 | 5.16 | ≈ 持平 |
| **SDE anneal. γ=2.0→0** | 32 | 32 | **21.47** | **5.10** | 🏆 32 NFE 最优 |
| | | | | | |
| exp_heun | 32 | 64 | 27.36 | 5.17 | ❌ |
| Heun | 32 | 64 | 28.42 | 5.18 | ❌ |
| pc γ=0.3 | 32 | 64 | 23.97 | 5.13 | ❌ |
| | | | | | |
| SDE γ=1.0 | 64 | 64 | **19.22** | **5.08** | ✅ 64 NFE 基线 |
| **SDE anneal. γ=2.0→0** | 64 | 64 | **15.71** | 5.01 | 🏆 最低 PPL |
| | | | | | |
| Heun | 64 | 128 | 17.95 | 5.02 | ❌ 2× NFE |
| | | | | | |
| SDE anneal. γ=2.0→0 | 128 | 128 | 11.32 | 4.90 | 💀 坍缩 |
| SDE anneal. γ=2.0→0 | 256 | 256 | 9.08 | 4.82 | 💀 坍缩 |

### 6.2 条件生成总表

| 任务 | 方法 | NFE | BLEU | ROUGE-1 | ROUGE-L | 状态 |
|------|------|-----|------|---------|---------|------|
| XSum | ODE 64步 | 64 | 8.20 | **36.25** | **28.36** | ✅ 基线 |
| XSum | SDE anneal. 32步 | **32** | 7.73 | 35.56 | 27.77 | 🏆 半算力 98% 质量 |
| WMT | ODE 64步 | 64 | 25.71 | 59.02 | 53.67 | ✅ 基线 |
| WMT | **SDE anneal. 32步** | **32** | **26.62** | **59.41** | **53.98** | 🏆 半算力反超 |

---

## 七、核心结论

1. **SDE 噪声注入是 ELF 采样质量的核心机制。** 所有纯确定性方法（Heun, exp, exp_heun, 自适应 Heun）在同 NFE 下均显著差于 SDE。高阶精度和自适应步长在奇异性速度场下无法替代随机噪声。

2. **SDE annealing 是最优采样策略。** 配方：$\gamma_{\text{start}}{=}2.0$，$\gamma_{\text{end}}{=}0.0$，线性退火 ($p{=}1$)，64 步。在 64 NFE 下达到 PPL=15.71（比原版 SDE 64 步的 19.22 降低 18.3%）。消融实验证实增益来自**噪声预算的时域重分配**——同等预算下，前移噪声比均匀分配或缩预算方案均更优。

3. **存在最优步数 N*≈40-80。** 步数过少 (N=32) 积分误差主导，步数过多 (N=256) 噪声被稀释导致坍缩。详见 THEORY.md 第 3.2 节。

4. **解码后处理不能挽救已坍缩的 latent。** information loss 发生在潜空间，token 级的 T, rp, lns 后处理无效——必须从源头（采样器）保多样性。

5. **噪声"在哪里、多大"是关键。** 自条件通道 (sc_noise) 和微小 z_noise (0.05-0.2) 均无效。必须是 SDE 量级的 z 空间噪声 (gamma=1.0-2.0)，配合退火协议管理 t→1 的收敛质量。

6. **PPL 和 Entropy 的关系**：在所有实验中，两者被噪声量级同向驱动——噪声多则 PPL 和 Entropy 均升高，噪声少则均降低。这并非数学上的互斥约束，而是 ELF-B 噪声机制的经验规律。理论上存在同时优化两者的空间（更大模型、更好训练），但在我们的采样方法框架内，SDE annealing 给出了当前模型容量下的最佳权衡点。

7. **方法探索路径总结**：
   ```
   尝试提高确定性精度 → 二阶RK、指数积分、自适应步长 → PPL降但Entropy也降
   尝试微量随机扰动    → sc_noise、z_noise、PC → 量级太小无效
   尝试超量随机扰动    → sde_ml k=2,3 → 坍缩
   找到甜点             → SDE annealing gamma=2.0→0, N=64 → 无条件生成最优
                       → SDE annealing gamma=2.0→0, N=32 → 翻译任务反超基线
   ```

8. **跨模型规模验证**：SDE annealing 在 ELF-B (105M) 和 ELF-L (652M) 上均一致提升无条件生成 PPL。改进幅度与训练充分程度正相关（ELF-B 5 epoch: -18.3% > ELF-L 3 epoch: -7.3%）。

9. **条件生成的任务依赖性**：摘要 (XSum) 对采样方法不敏感，ODE 32 NFE 即为 Pareto 最优。翻译 (WMT) 是唯一 SDE annealing 在条件生成上实现质量+速度双赢的任务——32 NFE BLEU=26.62 反超 ODE 64 NFE 的 25.71，同时加速 2.6×。可能因翻译的受限输出空间 (128 tokens) 使 SDE 噪声能在语义约束内探索有益多样性。

---

## 八、跨模型验证

### ELF-L 结果

| 方法 | NFE | PPL | Entropy |
|------|-----|-----|---------|
| SDE γ=1.5 | 32 | 30.53 | 5.35 |
| SDE anneal. 2.0→0 | 32 | 28.88 | 5.32 |
| SDE γ=1.0 | 64 | 23.88 | 5.29 |
| mild anneal. 1.5→0 p=2 | 64 | 23.21 | 5.26 |
| SDE anneal. 2.0→0 | 64 | **22.13** | 5.26 |

**发现**：SDE annealing 在 ELF-L (652M) 上仍然有效——64 NFE 下 PPL=22.13 vs SDE 的 23.88 (-7.3%)；32 NFE 下 28.88 vs SDE 的 30.53 (-5.4%)。改进幅度小于 ELF-B (-18.3%)——ELF-L 仅训练了 3 epoch，去噪网络不够成熟，限制了噪声调度的收益空间。更好的训练应能放大 SDE annealing 的优势。

### 跨模型规模总结

| 模型 | 参数量 | 训练 | SDE 64 PPL | SDE anneal. 64 PPL | Δ% |
|------|--------|------|-----------|-------------------|-----|
| ELF-B | 105M | 5 epoch | 19.22 | **15.71** | -18.3% |
| ELF-L | 652M | 3 epoch | 23.88 | **22.13** | -7.3% |

两个规模上 SDE annealing 均一致提升。改进幅度与训练充分程度正相关（B 5 epoch > L 3 epoch）。

---

## 九、条件生成任务

> **研究动机**：SDE annealing 在无条件生成上表现优异，但在条件生成（摘要、翻译）中 SDE 噪声是否会扰动条件信号？ODE 作为原版默认方法在 XSum 上是唯一选项。我们在两个条件任务上对比 ODE baseline vs SDE vs SDE annealing。

### 9.1 XSum 摘要 (ELF-B)

**配置**：max_length=1088, max_input_length=1024, CFG=2, SC-CFG=1, seed=42。指标为 BLEU 和 ROUGE。

| 方法 | NFE | BLEU | ROUGE-1 | ROUGE-2 | ROUGE-L | 时间 |
|------|-----|------|---------|---------|---------|------|
| ODE 64步 (基线) | 64 | 8.20 | **36.25** | **12.51** | **28.36** | 297s |
| SDE anneal. 2.0→0 32步 | **32** | 7.73 | 35.56 | 11.87 | 27.77 | **145s** |

**发现**：SDE annealing 32 NFE 在只需一半算力的情况下达到基线 98% 的 ROUGE-1（35.56 vs 36.25）和 98% 的 ROUGE-L（27.77 vs 28.36）。时间从 297s 降至 145s，**2× 加速**。对于摘要这类对采样方法不敏感的条件任务，半算力的 SDE annealing 是实用的 Pareto 最优选择。

**完整 XSum 对比**（含其他方法，供参考）：

| 方法 | NFE | ROUGE-1 | ROUGE-L |
|------|-----|---------|---------|
| ODE 64步 | 64 | 36.25 | 28.36 |
| Heun 64步 | 128 | 36.33 | 28.32 |
| SDE γ=1.0 64步 | 64 | 35.90 | 27.95 |
| SDE anneal. 2.0→0 64步 | 64 | 36.07 | 28.14 |
| ODE 32步 | 32 | 36.00 | 27.89 |
| **SDE anneal. 2.0→0 32步** | **32** | **35.56** | **27.77** |

### 9.2 WMT14 De-En 翻译 (ELF-B)

**配置**：max_length=128, max_input_length=64, CFG=2, SC-CFG=1, seed=42。翻译任务序列短，生成速度快。

| 方法 | NFE | BLEU | ROUGE-1 | ROUGE-2 | ROUGE-L | 时间 |
|------|-----|------|---------|---------|---------|------|
| ODE 64步 | 64 | 25.71 | 59.02 | 33.08 | 53.67 | 50s |
| SDE anneal. 2.0→0 64步 | 64 | 26.50 | **59.44** | **33.47** | **54.32** | 38s |
| **SDE anneal. 2.0→0 32步** | **32** | **26.62** | 59.41 | 33.25 | 53.98 | **19s** |

**发现**：
1. **🏆 SDE annealing 32步全方位超越 ODE 64步。** BLEU=26.62 vs 25.71 (+3.5%)，耗时仅 19s vs 50s（**2.6× 加速**）。这是全部实验中唯一一个在条件生成上 SDE annealing 同时实现质量提升和计算节省的案例。
2. **翻译任务可能天然适合 SDE 噪声。** 较短的序列长度 (128 tokens) 和较小的输出空间使得 SDE 噪声能在有限范围内探索不同译文表述，而不偏离源语言语义约束。
3. **32步已饱和。** 64步 SDE annealing 的 BLEU (26.50) 反而略低于 32步 (26.62)——更多步骤的额外积分精度在翻译任务上无益。

### 9.3 无条件 vs 条件：方法推荐分歧

| 任务 | baseline (64 NFE) | 我们的 (32 NFE) | 优势 |
|------|------------------|-----------------|------|
| 无条件生成 | SDE γ=1.0, PPL=19.22 | SDE anneal., PPL=21.47 | 半算力, NFE 减半 |
| 无条件生成 | SDE γ=1.0, PPL=19.22 | SDE anneal. 64步, PPL=15.71 | 同算力, PPL -18.3% |
| XSum 摘要 | ODE, ROUGE-1=36.25 | SDE anneal., ROUGE-1=35.56 | 半算力, 质量几乎持平 (-2%) |
| WMT 翻译 | ODE, BLEU=25.71 | **SDE anneal., BLEU=26.62** | **半算力, 质量反超 (+3.5%)** |

**核心发现：同一模型、同一架构，不同任务的最优采样策略不同。** 无条件生成需要 SDE 噪声探索多模态；条件生成中，摘要任务对采样方法不敏感（条件信号锁住了输出空间），但翻译任务中 SDE annealing 32步实现了质量和速度的双重超越——可能因为更短的序列 (128 tokens) 让噪声在有限空间内探索不同译文表述而不偏离语义约束。

### 8.1 ELF-L (652M, 3 epochs)

官方发布的 ELF-L 仅训练了 3 个 epoch（step=57051），而 ELF-B 训练了 5 个 epoch（step=95085）。这导致 ELF-L 的绝对 PPL 数值高于 ELF-B，但两者间的相对趋势一致。

| 方法 | NFE | gamma | PPL | Entropy | vs SDE |
|------|-----|-------|-----|---------|--------|
| SDE | 32 | 1.5 | 30.53 | 5.35 | 基线 |
| SDE anneal. | 32 | 2.0→0 | **28.88** | 5.32 | **-5.4%** |
| SDE | 64 | 1.0 | 23.88 | 5.29 | 基线 |
| SDE anneal. | 64 | 2.0→0 | **22.13** | 5.26 | **-7.3%** |
| mild anneal. | 64 | 1.5→0 p=2 | 23.21 | 5.26 | -2.8% |

**发现**：

1. **SDE annealing 在 ELF-L 上持续改进 PPL**——在 32 NFE 下降低 5.4%，在 64 NFE 下降低 7.3%。方向与 ELF-B 完全一致。

2. **改进幅度小于 ELF-B**（ELFL: 7% vs ELF-B: 18%，64 NFE）。一个可能的解释是 ELF-L 训练不充分（仅 3 epochs），模型本身的去噪能力较弱，导致采样器差异被模型噪声掩盖。更大的模型在充分训练后可能更受益于采样器优化。

3. **ELF-L 的 Entropy 系统性高于 ELF-B**（5.26-5.35 vs 5.01-5.17）——更大模型天然产生更多样化的文本，即使训练不充分。

4. **p=2 退火在 ELF-L 上表现更差**，与 ELF-B 的趋势一致：线性退火 (p=1) 在 gamma=1.5 和 gamma=2.0 时均优于二次退火。

### 8.2 ELF-M (342M, 4 epochs)

| 方法 | NFE | gamma | PPL | Entropy | vs SDE |
|------|-----|-------|-----|---------|--------|
| SDE | 32 | 1.5 | 26.50 | 5.24 | 基线 |
| SDE anneal. | 32 | 2.0→0 | **25.09** | 5.23 | **-5.3%** |
| SDE | 64 | 1.0 | 21.52 | 5.18 | 基线 |
| SDE anneal. | 64 | 2.0→0 | **18.76** | 5.14 | **-12.8%** |
| mild anneal. | 64 | 1.5→0 p=2 | 20.87 | 5.18 | -3.0% |

### 8.3 三模型综合对比 (64 NFE)

| 模型 | 参数量 | epochs | SDE PPL | SDE anneal. PPL | 提升 | SDE Ent | SDE anneal. Ent |
|------|--------|--------|---------|---------------|------|---------|---------------|
| ELF-B | 105M | 5 | 19.22 | **15.71** | **-18.3%** | 5.08 | 5.01 |
| ELF-M | 342M | 4 | 21.52 | **18.76** | **-12.8%** | 5.18 | 5.14 |
| ELF-L | 652M | 3 | 23.88 | **22.13** | **-7.3%** | 5.29 | 5.26 |

**发现**：

1. **SDE annealing 全模型一致改善**——3/3 模型规模，PPL 降低 7-18%，方向统一。

2. **提升幅度与训练量正相关**。ELF-B (5 epochs) 受益最大 (-18.3%)，ELF-L (3 epochs) 受益最小 (-7.3%)。直觉：训练更充分的模型产生的 x0_pred 更准确，因此采样精度对最终文本质量的影响更大。欠训模型的去噪网络自身误差主导了 PPL，采样器差异被淹没。

3. **Entropy 随模型规模递增**。ELF-B: 5.01-5.08, ELF-M: 5.14-5.18, ELF-L: 5.26-5.29。这验证了熟知的缩放律：更大模型产生更多样化的文本。

4. **ELF-M 处于甜点**——4 epochs 训练 + 342M 参数量，在 PPL 和 Entropy 之间取得了最好的平衡。如果 ELF-B 训练到 5 epochs 的 PPL=15.71 是最低绝对值，ELF-M 的 PPL=18.76 配合 5.14 的 Entropy 是更"自然"的选择。

---

## 十、自回归模型基线

**评估器**: GPT-2 Large | **协议**: 无条件生成 1000 条 × max_length=1024, T=1.0, seed=42

### 10.1 Pythia（同 GPT-2 tokenizer，The Pile 训练）

| 模型 | 参数量 | PPL ↓ | Entropy ↑ |
|------|--------|-------|-----------|
| Pythia-160M | 160M | 12.53 | 4.31 |
| Pythia-410M | 410M | 10.00 | 4.44 |
| Pythia-1.4B | 1.4B | 10.29 | 4.48 |

### 10.2 OPT（不同 tokenizer）

| 模型 | 参数量 | PPL ↓ | Entropy ↑ |
|------|--------|-------|-----------|
| OPT-125M | 125M | 13.01 | 4.04 |
| OPT-350M | 350M | 11.43 | 4.02 |
| OPT-1.3B | 1.3B | 13.66 | 4.02 |

### 10.3 GPT-2（同 tokenizer）

| 模型 | 参数量 | PPL ↓ | Entropy ↑ |
|------|--------|-------|-----------|
| GPT-2 Small | 124M | 13.16 | 4.76 |
| GPT-2 Medium | 355M | 12.09 | 4.97 |
| GPT-2 Large | 774M | 7.71 | 4.89 |
| GPT-2 XL | 1.5B | 8.63 | 4.89 |

### 10.4 ELF（SDE annealing 64 步，参考）

| 模型 | 参数量 | PPL ↓ | Entropy ↑ |
|------|--------|-------|-----------|
| ELF-B | 105M | 15.71 | 5.01 |
| ELF-M | 342M | 18.76 | 5.14 |
| ELF-L | 652M | 22.13 | 5.26 |
